# Conda env: conda env create -f environment.yml && conda activate nyc-tree-explorer
name: nyc-tree-explorer
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - streamlit>=1.28,<2
  - pandas>=2,<3
  - matplotlib>=3.7,<4
  - pydeck>=0.8,<1
  - requests>=2.31,<3
  - pyarrow>=14,<20
